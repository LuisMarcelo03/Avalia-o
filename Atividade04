#include <stdio.h>
#include <locale.h>

int main(){
	setlocale(LC_ALL, "portuguese");
	int n;
	
	printf("Digite um número inteiro:  ");
	scanf("  %d", &n);
	
	while(n >=0){
		printf("%d  ", n);
		n--;
	}
}
